/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.pinimg.com'],  // Ajoutez ici les domaines autorisés pour les images
      },
};

export default nextConfig;
